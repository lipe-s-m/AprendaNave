// src/environments/environment.prod.ts (Ajuste o valor para o alvo do sed)
export const environment = {
  production: true, // 💡 Use o valor de DEV como placeholder para que o script possa encontrá-lo
  apiUrl: 'http://localhost:5269',
};
